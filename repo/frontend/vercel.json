{
  "buildCommand": "ng build --configuration production && ng build --configuration production,en --output-path dist/en-temp && cp -r dist/en-temp/browser/en dist/crypto-dashboard-frontend/browser/ && rm -rf dist/en-temp",
  "outputDirectory": "dist/crypto-dashboard-frontend/browser",
  "rewrites": [
    { "source": "/en/(.*)", "destination": "/en/index.html" },
    { "source": "/(.*)",    "destination": "/index.html"    }
  ]
}
